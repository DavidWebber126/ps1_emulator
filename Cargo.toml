[package]
name = "ps1_emulator"
version = "0.1.0"
edition = "2024"

[dependencies]
eframe = "0.33.3"
tracing = { version = "0.1.44", features = ["max_level_trace", "release_max_level_info"] }
tracing-subscriber = { version = "0.3.22", features = ["registry", "env-filter"] }
